#include<stdio.h>
#include<math.h>
int main(){
  int n
  
    for(int i = 2; i <= sqrt(n); i++)
    {
        if(n % i == 0)
        {
            printf("khong la so nguyen to");
        }else 
        {
            printf("la so nguyen to");
        }
        
    return 0;}
    
    }
    
   int n;
   scanf("%d", &n);
    int a[10000];
    
    int dem = 0;
    for(int i = 0; 1 < n; i++){
        if(nt(a[i])){
            ++dem;
        }
    
return 0;
}